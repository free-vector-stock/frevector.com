# Frevector Redirects
# SPA fallback - route all non-file requests to index.html for client-side routing
# This ensures /details/* and other paths return 200 OK with index.html
/*  /index.html  200

# API routes are handled by Cloudflare Pages Functions automatically
# No need for explicit redirects here as they take precedence
